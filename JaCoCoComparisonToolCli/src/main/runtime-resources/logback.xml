<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <contextName>JacocoComparisonTool (${HOSTNAME})</contextName>

  <!-- Insert the current time formatted as "yyyyMMdd'T'HHmmss" under
    the key "bySecond" into the logger context. This value will be
    available to all subsequent configuration elements. -->
  <timestamp key="byMinute" datePattern="yyyyMMdd'T'HHmm" />

  <appender name="FILE" class="ch.qos.logback.core.FileAppender">
    <!-- use the previously created timestamp to create a uniquely
      named log file -->
    <file>JacocoComparisonTool-${byMinute}.log</file>
    <encoder>
      <pattern>%date \(%6relative ms\) %-5level %45logger{45} - %msg%n</pattern>
    </encoder>
  </appender>

  <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
   <!-- encoders are assigned the type
        ch.qos.logback.classic.encoder.PatternLayoutEncoder by default -->
    <encoder>
   <!--
     <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
   -->
   <!--
      <pattern>%d{ISO8601} %r %-5p %c %m%n</pattern>
    -->
      <pattern>%d{ISO8601} %-5level %logger{50} - %msg%n</pattern>
    </encoder>
  </appender>

  <root level="TRACE">
    <appender-ref ref="FILE" />
    <appender-ref ref="STDOUT" />
  </root>

  <logger name="edu.cmu.jacoco" level="INFO" />

</configuration>